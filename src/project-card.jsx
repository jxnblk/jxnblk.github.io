
var React = require('react');

module.exports = React.createClass({

  render: function() {
    var classes = {
      section: '',
    };
    return (
      <section className={classes.section}>
      </section>
    )
  }

});
